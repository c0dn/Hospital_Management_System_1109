package policy;

public class HealthInsurancePolicy extends InsurancePolicy {
    //still not sure need to have this but if have this we will be moving alot of stuff such as deductible here
    //we will add more policy like critical illness if so. will do at sat afternoon
}
